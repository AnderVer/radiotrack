# 🧪 RadioTrack — Быстрый старт (локальное тестирование)

**Дата:** 18 февраля 2026

---

## 🚀 Пошаговая инструкция

### Шаг 1: Запуск Docker

```powershell
cd "c:\Акселерация Обучение\AutoRadio\RadioTrack"
docker compose up -d --build
```

**Ожидание:** 5-15 минут (первый build)

### Шаг 2: Проверка контейнеров

```powershell
docker compose ps
```

**Ожидаемый результат:**
```
NAME                IMAGE              STATUS
radiotrack-db-1     postgres:16        Up
radiotrack-web-1    radiotrack-web     Up
```

### Шаг 3: Миграция БД

```powershell
docker compose exec web bin/rails db:create db:migrate
```

### Шаг 4: Посев данных

```powershell
docker compose exec web bin/rails db:seed
```

**Ожидаемый результат:**
- 10 станций создано
- Sample detections добавлены

### Шаг 5: Проверка в консоли

```powershell
docker compose exec web bin/rails console
```

```ruby
# Проверка станций
Station.count  # => 10
Station.first.title  # => "Авторадио"

# Проверка detections
Detection.count  # => 100+

# Выход из консоли
exit
```

### Шаг 6: Открыть приложение

```
http://localhost:3000
```

---

## ✅ Чек-лист тестирования

### Базовый UI

- [ ] Главная страница отображается
- [ ] Видно 10 станций
- [ ] У станций видны последние треки
- [ ] Кнопки "Открыть" и "Подробнее" работают

### Навигация

- [ ] Клик на станцию → страница станции
- [ ] Видны последние 20 треков
- [ ] Кнопки "Открыть сайт" и "Слушать" работают

### Регистрация/Вход

- [ ] Клик "Регистрация" → форма
- [ ] Регистрация с email/password работает
- [ ] После регистрации redirect на главную
- [ ] В меню видно "Профиль" и "Выйти"

### Профиль

- [ ] Клик "Профиль" → страница профиля
- [ ] Видно email и план (Guest/Auth)
- [ ] Кнопка "Активировать демо-подписку" работает
- [ ] После активации видно "Premium активна"

### Закладки

- [ ] На странице станции клик "☆ В закладки"
- [ ] Redirect на закладки
- [ ] Станция видна в списке закладок
- [ ] Кнопка "★ В закладках" работает
- [ ] Клик "Удалить" удаляет закладку

### Плейлисты

- [ ] Клик "Плейлисты" → список
- [ ] Клик "➕ Создать плейлист" → форма
- [ ] Создание плейлиста работает
- [ ] Плейлист виден в списке

---

## 🐛 Отладка

### Если Docker не запускается

```powershell
# Проверить логи
docker compose logs web

# Перезапустить
docker compose down
docker compose up -d --build
```

### Если БД не подключается

```powershell
# Удалить volumes и пересоздать
docker compose down -v
docker compose up -d
docker compose exec web bin/rails db:create db:migrate db:seed
```

### Если ошибки миграции

```powershell
# Проверить статус миграций
docker compose exec web bin/rails db:migrate:status

# Откатить последнюю
docker compose exec web bin/rails db:rollback

# Применить все
docker compose exec web bin/rails db:migrate
```

---

## 📊 Ожидаемые результаты

### После db:seed

```ruby
Station.count  # => 10
Detection.count  # => 100+
```

### После активации демо-подписки

```ruby
user = User.first
user.plan_code  # => "paid"
user.paid_until  # => 30.days.from_now
user.can_access_last_track?  # => true
```

### После AvtoradioIngestJob

```ruby
AvtoradioIngestJob.perform_now
# => { station_id: 1, tracks_parsed: 4, tracks_created: 4, tracks_skipped: 0 }

Detection.where(source: "avtoradio_playlist").count  # => 4+
```

---

## 🎯 Критерии успешного тестирования

**M0-M9 считаются протестированными, если:**

1. ✅ `docker compose up -d` запускает оба контейнера
2. ✅ `http://localhost:3000` открывает главную страницу
3. ✅ Видны 10 станций с последними треками
4. ✅ Регистрация работает
5. ✅ Вход через email/password работает
6. ✅ Профиль отображается
7. ✅ Демо-подписка активируется
8. ✅ Закладки работают (добавить/удалить)
9. ✅ Плейлисты создаются
10. ✅ `AvtoradioIngestJob.perform_now` создаёт detections

---

**Готово к тестированию!** 🚀
