<% content_for :title, "Вход — RadioTrack" %>

<div class="main" style="max-width: 500px; margin: 0 auto;">
  <h1 class="mb-3" style="text-align: center;">Вход в аккаунт</h1>

  <%= form_for(resource, as: resource_name, url: session_path(resource_name), html: { class: "mb-4" }) do |f| %>
    <div class="mb-3">
      <%= f.label :email, class: "form-label" %>
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control", style: "width: 100%; padding: 8px; border: 1px solid #ddd; border-radius: 4px;", placeholder: "Email" %>
    </div>

    <div class="mb-3">
      <%= f.label :password, "Пароль", class: "form-label" %>
      <%= f.password_field :password, autocomplete: "current-password", class: "form-control", style: "width: 100%; padding: 8px; border: 1px solid #ddd; border-radius: 4px;", placeholder: "Пароль" %>
    </div>

    <% if devise_mapping.rememberable? %>
      <div class="mb-3">
        <label style="display: flex; align-items: center; gap: 8px;">
          <%= f.check_box :remember_me %>
          Запомнить меня
        </label>
      </div>
    <% end %>

    <div class="mb-3">
      <%= f.submit "Войти", class: "btn btn-primary", style: "width: 100%;" %>
    </div>
  <% end %>

  <div style="text-align: center; margin: 16px 0; color: #666;">
    <span>или войдите через</span>
  </div>

  <div style="display: flex; flex-direction: column; gap: 12px;">
    <%= button_to user_vk_omniauth_authorize_path, method: :post, data: { turbo: false }, class: "btn", style: "background: #0077FF; color: #fff; border: none; padding: 12px; border-radius: 4px; font-weight: 500;" do %>
      🔵 Войти через VK
    <% end %>

    <%= button_to user_yandex_omniauth_authorize_path, method: :post, data: { turbo: false }, class: "btn", style: "background: #FC3F1D; color: #fff; border: none; padding: 12px; border-radius: 4px; font-weight: 500;" do %>
      🔴 Войти через Yandex
    <% end %>
  </div>

  <div class="mt-4" style="text-align: center;">
    <p class="text-muted">
      Нет аккаунта?
      <%= link_to "Зарегистрироваться", new_registration_path(resource_name) %>
    </p>
    <p class="text-muted">
      <%= link_to "Забыли пароль?", new_password_path(resource_name) %>
    </p>
  </div>
</div>
