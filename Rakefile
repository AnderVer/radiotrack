# frozen_string_literal: true

# Disable bootsnap due to path encoding issues
# require_relative "config/application"
require File.expand_path("config/application", __dir__)

Rails.application.load_tasks
